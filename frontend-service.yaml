apiVersion: v1
kind: Service
metadata:
  name: tienda-frontend
  namespace: tienda
spec:
  selector:
    app: tienda-frontend
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer
